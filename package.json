{
  "name": "rust-rcon",
  "description": "RCON Client to interface with a Rust server.",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun run build.mjs",
    "prepublishOnly": "bun run build"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "bun-plugin-dts": "^0.2.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "events": "^3.3.0",
    "typed-emitter": "^2.1.0",
    "ws": "^8.16.0"
  }
}